# libbgp C11 Conversion Progress

Source plan: `/home/kshi/workspace/code/test/libbgp-gpt/docs/superpowers/plans/libbgp-c11-conversion-plan-v4.md`

Worktree: `/home/kshi/workspace/code/test/libbgp-gpt/worktrees/codex`
Branch: `codex_v2`

## Current Status

C++ parity follow-up fixes from the first two comparison passes are implemented and committed.

Third comparison pass parity fixes are implemented in the current worktree and recorded as completed in `TODO.md`:

- High: FSM route-event publication is now best-route aware and prefix scoped.
- High: ESTABLISHED teardown now publishes route withdraw/replacement events for learned best-route changes before `SESSION_DOWN`.
- Medium: initial post-establishment RIB advertisement now iterates only best/active RIB4/RIB6 routes.
- Medium: outbound UPDATE propagation now drops optional non-transitive attributes before nexthop/AS4/prepend preparation.
- Low/API: legacy 120/40 hold/keepalive defaults are restored for `libbgp_fsm_init(fsm, NULL)` through the internal default config path; caller-provided configs keep their explicit field values, including zero-initialized hold/keepalive values. Remaining driver/logging/state API-shape differences are documented in `README.md`.

Post-review fixes completed in this round:

- Journaled route events now publish in the same order that UPDATE contents are applied, covering mixed withdraw-and-add UPDATEs for the same prefix.
- `libbgp_fsm_stop()` now publishes route discard events before the shutdown Cease notification; `libbgp_fsm_reset_soft()` keeps the legacy notification-before-hard-reset behavior.
- RIB4/RIB6 best-route foreach helpers now snapshot routes under lock and invoke callbacks after unlocking, avoiding output-handler I/O or reentrant callbacks under the RIB lock.
- README/TODO/progress now explicitly document that `libbgp_fsm_init(fsm, NULL)` uses 120/40 defaults, while caller-provided zero-initialized configs preserve zero hold/keepalive values.
- Final review High: IPv6 UPDATE apply now handles all `MP_UNREACH` attributes before all `MP_REACH` attributes, matching legacy `BgpFsm::fsmEvalEstablished()` ordering even when attributes arrive in the reverse order.
- Final review Medium: initial RIB dump now checks `session_generation` before and after each per-route send, stops on reentrant teardown, and avoids publishing `SESSION_UP` for stale sessions.
- Final review Medium: RIB4/RIB6 best-route ordering now prefers lower/older `update_id`, matching legacy `BgpRibEntry::operator<()` tie-break behavior.

Verification for the final-review fixes is complete.

Second comparison pass residual fixes:

- MP-BGP IPv4/IPv6 route-family gating now follows legacy semantics:
  - `libbgp_fsm_set_mpbgp_ipv4()` enables IPv4 MP-BGP capability advertisement.
  - IPv6-only MP-BGP sessions skip IPv4 inbound NLRI, initial RIB4 advertisement, and route-event advertisement.
- IPv6 default nexthop rewrite now supports a configured link-local nexthop via `libbgp_fsm_set_default_nexthop6_linklocal()`.
- FSM collision detection can be disabled per FSM with `libbgp_fsm_set_no_collision_detection()`.
- Soft/hard reset parity is restored:
  - `libbgp_fsm_reset_soft()` sends Cease/Admin Reset.
  - `libbgp_fsm_reset_hard()` tears down without a notification.
- Observability parity is restored:
  - `libbgp_fsm_negotiated_hold_time()`.
  - `libbgp_fsm_set_state_change_cb()`.
- Remaining API-shape differences are documented in `README.md` as convenience-wrapper differences rather than core protocol behavior gaps.

Earlier C++ parity fixes:

- AS4-aware sink parsing via `libbgp_sink_init_as4()`.
- FSM route-event outbound UPDATE propagation while ESTABLISHED.
- Outbound UPDATE preparation with AS4 restore/downgrade and eBGP local-AS prepend.
- Expected peer ASN enforcement.
- Local-AS loop suppression with configurable allowance.
- Configurable learned-route weight.
- Default internal FSM RIB4/RIB6 ownership with public getters.
- IPv4 FSM in/out filter hooks.
- Expanded IPv4 filter operators, AS_PATH negative/origin matches, and COMMUNITY negative match.
- Local RIB4/RIB6 legacy path attributes for local routes.
- Initial FSM advertisement of existing eligible RIB4/RIB6 routes after session establishment.
- IPv4 and IPv6 nexthop validation/default rewrite controls.
- RIB6 link-local nexthop preservation for 32-byte MP_REACH nexthops.
- iBGP learned-route marking for IPv4/IPv6.
- FSM collision detection via `LIBBGP_EVENT_COLLISION`.
- IPv6 filter API and FSM in/out filter hooks.
- Path-attribute debug formatting via `libbgp_pattr_format()`.

Focused verification from the earlier parity round:

- `rtk make build/threadsafe-0/tests/test_sink && rtk build/threadsafe-0/tests/test_sink`
- `rtk make build/threadsafe-0/tests/test_fsm && rtk build/threadsafe-0/tests/test_fsm`
- `rtk make build/threadsafe-0/tests/test_filter && rtk build/threadsafe-0/tests/test_filter`
- `rtk make build/threadsafe-0/tests/test_rib`
- `rtk build/threadsafe-0/tests/test_rib`
- `rtk make build/threadsafe-0/tests/test_pattr`
- `rtk build/threadsafe-0/tests/test_pattr`

Verification after the third comparison pass fixes:

- `rtk build/threadsafe-0/tests/test_fsm` RED check failed as expected for `fsm_established_mp_reach6_before_unreach6_keeps_reached_route` before the IPv6 apply-order fix: expected route count 1, got 0.
- `rtk build/threadsafe-0/tests/test_fsm` RED check failed as expected for `fsm_open_confirm_initial_rib_dump_stops_after_reentrant_stop` before the per-route session-generation fix: expected 1 UPDATE send, got 2.
- RIB worker RED check failed as expected for `rib4_best_route_prefers_lower_update_id` before the update_id tie-break fix: the newer route won.
- `rtk make build/threadsafe-0/tests/test_fsm && rtk build/threadsafe-0/tests/test_fsm` passed with `fsm: 127 tests`.
- `rtk make build/threadsafe-0/tests/test_rib && rtk build/threadsafe-0/tests/test_rib` passed with `rib: 13 tests`.
- `rtk make test` passed.
- `rtk make verify` passed.
- `rtk git diff --check` passed with no output.
- `rtk rg -n "\\[ \\]" TODO.md progress.md` returned no unchecked items.
- `rtk git status --short --branch` shows modified implementation/docs files plus untracked `TODO.md` and `progress.md`.
- A parallel targeted build attempt hit a build-directory `.flags` temporary-file race while running FSM and RIB make targets concurrently; the same FSM target was rerun serially and passed.

Next:

- Final response should summarize the completed High/Medium/Low fixes and verification evidence.
- Keep `TODO.md` and `progress.md` untracked/controller-owned unless the user asks to include them.

## Post-review Medium/Low Priority Fixes

- RIB local duplicate insert behavior now matches legacy C++:
  - `libbgp_rib4_insert_local()` and `libbgp_rib6_insert_local()` reject duplicate source-0 prefixes with `LIBBGP_ERR_EXISTS`.
  - Added IPv4 and IPv6 regression tests.
- Event bus publisher exclusion restored for C API users:
  - Added `libbgp_event_bus_publish_from(bus, publisher_id, event)`.
  - Existing `libbgp_event_bus_publish()` remains the no-publisher wrapper.
  - Added publisher-exclusion regression coverage.
- Build/release hygiene:
  - `make all` now provides `build/libbgp.a` and `build/libbgp.so` stable aliases for the active build configuration.
  - Added `make symbol-check`, `make verify`, and `make release-check`.
  - `symbol-check` rejects private RIB helpers that accidentally use public-looking `libbgp_` names.
- Private RIB helper symbols were renamed from `libbgp_rib*` to `bgp_rib*`.
- `include/libbgp/out_handler.h` no longer exposes `<sys/types.h>` or `ssize_t`; custom I/O callbacks return `libbgp_io_result_t`.

Verification after medium/low-priority fixes:

- `rtk make verify`
- `rtk sh -c '! rtk proxy rg -n "#include <sys/types\\.h>|ssize_t" include/libbgp/out_handler.h'`
- `rtk sh -c '! rtk proxy nm -g build/threadsafe-0/libbgp.a | rtk proxy rg " T libbgp_rib[46]_(saved_route|insert_save|withdraw_exact|restore_saved|exact_update)"'`
- `rtk git diff --check`

## Baseline Notes

- Created worktree at `./worktrees/codex` on branch `codex`.
- The repository starts as the legacy C++/Autotools tree.
- There is no committed `include/`, `tests/`, or root plain `Makefile`.
- Baseline `rtk make test` fails because the legacy tree has no `test` target.
- The v4 plan file exists in the parent checkout under `docs/`; it is used as the source of truth.

## Completed

- Parent checkout: added `worktrees/` to `.gitignore` and committed `chore: ignore local worktrees` before creating the worktree.
- Worktree setup complete.
- Phase 0-2: C11 bootstrap, allocator, error strings, internal helpers, vector, Makefile, and initial tests.
  - Commits:
    - `4343a7ffcef4e943dff636107c0f2e97a300159e` `feat: bootstrap c11 build and allocator core`
    - `80ea56f3ee1cd92b04d232ea6996e9bfb6319e2d` `fix: harden c11 bootstrap portability`
    - `bb85644d7042fb5727beed9fb3efd49151e290eb` `fix: preserve vector storage on reserve failure`
    - `fe39158707a9abe3c7701dcd394add887c894a0a` `fix: remove vector void pointer outputs`
  - Spec review: passed.
  - Code quality review: passed.
- Phase 3: logging API and implementation.
  - Commits:
    - `7974cd4070e44a9e37c9d5bc4365307f2859457b` `feat: add c11 logging API`
    - `dcc8926569fb7bddc99f416bc9da2e5d397680c6` `fix: synchronize default logger access`
  - Spec review: passed.
  - Code quality review: passed.
- Phase 4: IPv4 and IPv6 prefixes.
  - Commit: `91d4868de6c30f2a38b49e915e05030082474379` `feat: add c11 prefix parsers`
  - Spec review: passed.
  - Code quality review: passed.
- Phase 5: BGP capabilities.
  - Commits:
    - `489845d26320b4bdb5b47abc4d972789fe2334a0` `feat: add c11 bgp capabilities`
    - `594546c1b90c8e418d0c4840d85a419d442b8630` `test: cover capability allocation failure`
  - Spec review: passed.
  - Code quality review: passed.
- Phase 6: Path attributes.
  - Commits:
    - `79b4f2b7f59bf380c6f20ff38475e2d3233ec266` `feat: add c11 path attributes`
    - `ba933428632784df4883ed5a64c19082b5c85b5c` `fix: normalize path attribute write errors`
    - `44e8dfbac5f9d0af9260fea03b6684815bf424c7` `fix: harden path attribute validation`
    - `ac2e89171f453b598dcf1ed04761260243d6a17e` `fix: validate path attribute write flags`
  - Spec review: passed.
  - Code quality review: passed.
- Phase 7: OPEN, KEEPALIVE, NOTIFICATION, UPDATE, PACKET.
  - Commits:
    - `4f18534db1670bf38bfb03b3a844b89e67bd742c` `feat: add c11 bgp messages`
    - `ca9e007c7a00d75472ff0252969f02d5717d9051` `fix: normalize bgp message error codes`
    - `bd3a57d0744e26b34a985bff8256427062ddcc83` `fix: stage bgp packet writes before output`
  - Spec review: passed.
  - Code quality review: passed.
- Phase 8: Hashmap and RIB4/RIB6.
  - Commits:
    - `a43c9beba9cb44d3353a6c233a39f1440362a970` `feat: add c11 rib storage`
    - `6bae565ae2d9447bddf1605961a4d4c6dca1f99a` `fix: clarify rib lookup lifetime`
    - `8107c5ad9de36b7ef64ac02391c36ab32707dc9a` `fix: compare rib router ids in network order`
  - Spec review: passed.
  - Code quality review: passed.
- Phase 9: Filter and event bus.
  - Commits:
    - `af19c720196b7b29a64bae2d11d4d9696ace88ba` `feat: add c11 filters and events`
    - `0a31bb74cae520508ae3a31cbca70068a908d000` `fix: harden filters and event teardown`
    - `d685b29d3108e0e7ea5df2c4a67f2d131f19841a` `fix: synchronize filter and event teardown`
  - Spec review: passed.
  - Code quality review: passed.
- Phase 10: Sink and output handler.
  - Commits:
    - `45b70c2fab730cdbb5f8c2d6348170e21e083001` `feat: add c11 sink and output handler`
    - `5031a858fe6dfec5e4d3454198ca3a1772033e54` `fix: harden sink pop and output callbacks`
  - Spec review: passed.
  - Code quality review: passed.

## In Progress

- Phase 11: FSM.
  - Implementation commit: `05a5ea1db2c38d9c9dd9bac69939bf235e0a1b35`.
  - Spec compliance review: fixes required.
  - Accepted fix: validate OPEN hold time and BGP ID before accepting peer.
  - Rejected review suggestion: IDLE receiving OPEN remains accepted per Phase 11 task prompt.
  - Fix commit: `6db791462a08afbea3fdf2f574ee6b1686a6b8f6`.
  - Spec re-review: passed.
  - Code quality review: fixes required.
  - Required fixes dispatched to worker:
    - OPEN validation failures must send OPEN Message Error (`code=2`) with exact subcodes for version, BGP ID, and hold time.
    - `libbgp_fsm_start()` must not leave the FSM in `OPEN_SENT` if sending OPEN fails.
    - Invalid or unexpected packets must not refresh `last_rx_ms` and extend hold timer lifetime.
  - Requested tests: start send failure, invalid packet hold timer behavior, exact NOTIFICATION code/subcode for invalid OPEN fields.
  - Code quality fix commit: `3797e952aa24fa712e5dcbf89da49c4778830f0e` `fix: harden fsm error handling`.
  - Controller verification after fix: passed normal, headers, THREADSAFE, and sanitizer gates.
  - Spec compliance re-review: passed.
  - Code quality re-review: fixes required.
  - Second code-quality fixes dispatched to worker:
    - Allow syntactically valid unsupported-version OPEN packets to parse so FSM can send OPEN Message Error `2/1` on the real wire/parser path.
    - Close `libbgp_fsm_start()` reentrant/THREADSAFE race by marking an in-progress state before unlock and avoiding success-path state regression.
  - Second code-quality fix commit: `88343e65d242a087c9921c47d97a1b03eafdaf93` `fix: close fsm parser and start races`.
  - Controller verification after second fix: passed normal, headers, THREADSAFE, and sanitizer gates.
  - Second spec compliance re-review: dispatched.
  - Second code quality re-review: fixes required.
  - Third code-quality fix dispatched to worker:
    - `fsm_send_packet()` must handle short writes from `libbgp_out_handler_send()` instead of treating partial sends as success.
    - Add short-send start regression coverage and accepted-traffic hold-timer refresh coverage.
  - Third code-quality fix commit: `49f65444fab0d084e1c5a8d28cea61ff7be716de` `fix: handle fsm short sends`.
  - Spec re-review after second fix: fixes required.
  - RIB6 integration fix dispatched to worker:
    - Add `libbgp_fsm_set_rib6()`.
    - Apply `MP_REACH_IPV6` / `MP_UNREACH_IPV6` UPDATE attributes to RIB6.
    - Add FSM tests for RIB6 insert and withdraw.
  - RIB6 integration fix commit: `fc9b41ab83cf546ccd7a9a4c8008a2ae7e85f4cf` `fix: integrate fsm with rib6 updates`.
  - Controller verification after RIB6 fix: passed normal, headers, THREADSAFE, and sanitizer gates.
  - Final code quality review: fixes required.
  - Protocol cleanup dispatched to worker:
    - Discard RIB4/RIB6 learned routes on ESTABLISHED teardown.
    - Complete passive IDLE OPEN handling by sending local OPEN reply.
    - Send FSM Error `5/1`, `5/2`, `5/3` for unexpected messages in OpenSent/OpenConfirm/Established.
    - Schedule keepalives from negotiated hold time and disable periodic keepalives when hold time is 0.
    - Document borrowed attached FSM dependencies.
  - Protocol cleanup fix commit: `4d1d791424d16eecf79d4c154a6d51263c05b851` `fix: harden fsm teardown and protocol errors`.
  - Controller verification after protocol cleanup: passed normal, headers, THREADSAFE, and sanitizer gates.
  - Final spec compliance review: fixes required.
  - Required fix dispatched to worker:
    - `libbgp_fsm_stop()` must publish `LIBBGP_EVENT_SESSION_DOWN` when stopping an ESTABLISHED session after snapshotting the attached event bus.
  - Stop/session-down fix commit: `6b86d67322a12e6be3031da386f3dd8376c05b3a` `Publish session down on FSM stop`.
  - Controller verification after stop/session-down fix: passed normal, headers, THREADSAFE, and sanitizer gates.
  - Spec re-review after stop/session-down fix: fixes required.
  - Required fix dispatched to worker:
    - `fsm_effective_keepalive_time()` must treat `config.keepalive_time == 0` as defaulting to negotiated `hold/3` when negotiated hold is nonzero, while still disabling periodic keepalives when negotiated hold is 0.
  - Keepalive-default fix commit: `091ccf2f8b854802dbd50629930fc7d1d6e12faa` `Fix FSM keepalive default scheduling`.
  - Controller verification after keepalive-default fix: passed normal, headers, THREADSAFE, and sanitizer gates.
  - Spec re-review after keepalive-default fix: fixes required.
  - Required fix dispatched to worker:
    - Invalid UPDATE contents must not refresh `last_rx_ms`; `fsm_on_established()` currently marks rx before `fsm_apply_update()` can reject an UPDATE.
  - Invalid-update hold-timer fix commit: `ec0f037d11c5ad27497ab96e3313bf0e94fa011e` `Refresh hold timer after valid update`.
  - Controller verification after invalid-update hold-timer fix: passed normal, headers, THREADSAFE, and sanitizer gates.
  - Final spec compliance review after invalid-update hold-timer fix: passed.
  - Final code quality review: fixes required.
  - Required fixes dispatched to worker:
    - Roll back/teardown `OPEN_CONFIRM` state if `fsm_on_open_sent()` sends KEEPALIVE and it fails.
    - Roll back/teardown passive IDLE OPEN handling if local OPEN or KEEPALIVE send fails.
    - Treat missing output handler as `LIBBGP_ERR_INVALID` for required sends rather than silent success.
    - Prevent THREADSAFE UPDATE-vs-teardown stale route insertion, e.g. via session generation recheck/cleanup.
  - Code-quality fix commit: `ffd134b9fcf1e4c7f72552da1046ba000a6bf91b` `Fix FSM send failure teardown`.
  - Controller verification after code-quality fix: passed normal, headers, THREADSAFE, and sanitizer gates.
  - Spec re-review after code-quality fix: fixes required.
  - Required fixes dispatched to worker:
    - Guard `libbgp_fsm_start()` rollback with the session generation captured before sending OPEN.
    - Run late UPDATE stale route cleanup even when `fsm_apply_update()` returns an error after teardown.
  - Session-generation rollback/stale-update cleanup fix commit: `a4424155b18607937b86882b90d6681e7456574a` `Guard FSM session generation rollbacks`.
  - Controller verification after session-generation rollback fix: passed normal, headers, THREADSAFE, and sanitizer gates.
  - Spec review after session-generation rollback fix: fixes required.
  - Required fixes dispatched to worker:
    - Add real `CONNECT`/`ACTIVE` state support rather than enum-only placeholders.
    - Publish route add/withdraw events for successful IPv6 MP_REACH/MP_UNREACH RIB6 changes.
    - Expire hold timer at the negotiated deadline with `>=`.
  - CONNECT/ACTIVE, IPv6 route event, and hold-deadline fix commit: `9c81d3504eb36de505eb002f7739a0eea0bf235c` `Fix FSM connect active and IPv6 events`.
  - Controller verification after CONNECT/ACTIVE fix: passed normal, headers, THREADSAFE, and sanitizer gates.
  - Spec review after CONNECT/ACTIVE fix: passed.
  - Code quality review after CONNECT/ACTIVE fix: fixes required.
  - Required fixes dispatched to worker:
    - Increment/session-guard invalid passive OPEN paths.
    - Reserve generation/state during passive OPEN sends to prevent duplicate concurrent negotiation.
    - Add session-generation guards to OPEN_SENT KEEPALIVE and passive OPEN send-failure rollbacks.
    - Guard keepalive tick `last_keepalive_ms` update by captured session generation/state.
    - Do not publish unpaired `SESSION_DOWN` for hold timeout in `OPEN_CONFIRM`.
    - Extend route event payload so IPv6 route events identify the changed prefix.
  - Reentrant teardown/event payload fix commit: `a290e7c00f2aba5c359141c8516ef2521e8f9463` `Fix FSM reentrant teardown guards`.
  - Controller verification after reentrant teardown fix: passed normal, headers, THREADSAFE, and sanitizer gates.
  - Spec review after reentrant teardown fix: passed.
  - Code-quality review attempt after reentrant teardown fix failed due provider 429 before returning findings; retry required.
  - Code-quality retry after reentrant teardown fix: fixes required.
  - Required fixes dispatched to worker:
    - Publish IPv6 route events even when only an event bus is attached and `rib6 == NULL`, matching IPv4 event-bus-only behavior.
    - Replace stale UPDATE post-apply peer-wide discard with narrower/session-aware cleanup so reentrant callbacks cannot lose routes inserted by a newer same-peer session.
  - Stale-update narrow cleanup/event-bus-only IPv6 fix commit: `0bcd15bd2a1616ce017fc493fe8d57d7fb54dd31` `Fix FSM stale update route cleanup`.
  - Controller verification after stale-update cleanup fix: passed normal, headers, THREADSAFE, and sanitizer gates.
  - Spec review after stale-update cleanup fix: passed.
  - Code-quality review after stale-update cleanup fix: fixes required.
  - Required fixes dispatched to worker:
    - Stop stale UPDATE processing immediately after route-event callbacks/reentrant session generation changes, before applying later NLRI/MP routes.
    - Prevent invalid UPDATEs from partially mutating RIB/event state; validate first or roll back and defer events until success.
    - Validate MP_REACH/MP_UNREACH contents even when no RIB6 is attached.
  - Stale/invalid UPDATE application fix commit: `10829dffe70af10dca75d562f03d91fad1a11a41` `Fix stale FSM update application`.
  - Controller verification after stale/invalid UPDATE fix: passed normal, headers, THREADSAFE, and sanitizer gates.
  - Spec review after stale/invalid UPDATE fix: fixes required.
  - Required fixes dispatched to worker:
    - Reject route-advertising UPDATEs missing mandatory path attributes (`ORIGIN`, `AS_PATH`, and IPv4 `NEXT_HOP`; IPv6 MP_REACH uses MP nexthop but still requires common mandatory attrs).
    - Reject duplicate path attributes where duplicates would make UPDATE semantics ambiguous.
    - Add regression tests proving malformed UPDATEs do not mutate RIB, publish route events, or refresh hold timer.
  - Mandatory UPDATE attribute validation fix commit: `d64150611466c3d42012ce185b4b24d991191c86` `Validate mandatory UPDATE attrs in FSM`.
  - Controller verification after mandatory UPDATE attribute validation fix: passed normal, headers, THREADSAFE, and sanitizer gates.
  - Spec review after mandatory UPDATE attribute validation fix: fixes required.
  - Required fix dispatched to worker:
    - Invalid UPDATEs in ESTABLISHED must send UPDATE Message Error NOTIFICATION and transition to IDLE immediately instead of staying ESTABLISHED until hold timer expiry.
    - Regression tests must assert immediate NOTIFICATION/SESSION_DOWN behavior and preserve malformed UPDATE atomicity.
  - Invalid UPDATE notification/teardown fix commit: `dfc1d120bf3847a4432acc4e33aa6a73b40ee0de` `Handle invalid established updates`.
  - Controller verification after invalid UPDATE notification/teardown fix: passed normal, headers, THREADSAFE, and sanitizer gates.
  - Spec review after invalid UPDATE notification/teardown fix: passed.
  - Code-quality review after invalid UPDATE notification/teardown fix: fixes required.
  - Required fixes dispatched to worker:
    - Make UPDATE application transactional for non-`LIBBGP_ERR_INVALID` failures, including journal allocation/RIB insertion failures, so partial RIB4/RIB6 mutations are rolled back.
    - Reset peer/session fields when a NOTIFICATION is received before ESTABLISHED, without publishing an unpaired `SESSION_DOWN`.
    - Add regression coverage for both failure modes.
  - Transactional UPDATE/non-established NOTIFICATION cleanup fix commit: `5129fe565f6d19afe1be8e3582bbb2a6975677e7` `fix fsm update rollback on apply errors`.
  - Controller local review before verification found a remaining transactional UPDATE gap:
    - UPDATEs that withdraw existing routes before a later non-OK error can still lose the withdrawn route because rollback only tracks newly inserted routes.
    - Additional fix dispatched before running full controller gates.
  - Withdrawn-route rollback journal fix commit: `8b2f52c80d9cb562391eccc3b1c440087ec5c3f9` `Restore withdrawn routes on failed UPDATE`.
  - Controller local review before verification found a code-quality issue in the withdrawn-route rollback fix:
    - `src/fsm.c` now mirrors private RIB impl layouts and traverses `hashmap.h` internals; this breaks RIB encapsulation and can silently desynchronize if RIB internals change.
    - Cleanup fix dispatched to move exact lookup/snapshot operations behind private RIB helper functions while preserving rollback behavior.
  - RIB-private-helper cleanup commit: `6610a73a0c5aab0fde1a1b7cdee047aeb1afe8b1` `Refactor FSM rollback through RIB helpers`.
    - Added private `src/rib_internal.h` helpers for exact update-id lookup, update-id guarded withdrawal, detached withdrawal save, and restore-if-absent.
    - Removed direct `hashmap.h`/private RIB layout mirroring from `src/fsm.c`.
    - Kept withdrawn-route rollback behavior and adjusted the rollback allocation-failure regression to assert behavior instead of the old clone-based allocation count.
  - Controller verification after RIB-private-helper cleanup: passed normal, headers, THREADSAFE, and sanitizer gates.
  - Phase 11 final spec re-review after RIB-private-helper cleanup: fixes required.
    - IPv4 NLRI replacement followed by a later non-`LIBBGP_ERR_INVALID` UPDATE failure can lose the replaced route because rollback only journals the new update ID.
    - IPv6 MP_REACH replacement has the same gap.
    - Required fix: make same-peer/same-prefix replacement transactional too, with regression tests for IPv4 and IPv6 replacement rollback.
  - Replacement rollback fix commit: `237c45c85bfaa2bde2f29f52e034335de399e440` `Restore replaced routes on update rollback`.
    - Added private RIB insert helpers that return detached replaced routes without exposing them publicly.
    - FSM now journals replaced routes for IPv4 NLRI and IPv6 MP_REACH and restores them during non-invalid rollback.
    - Added IPv4 and IPv6 replacement rollback regression tests; FSM suite now has 61 tests.
  - Controller verification after replacement rollback fix: passed normal, headers, THREADSAFE, and sanitizer gates.
  - Phase 11 spec re-review after replacement rollback fix: passed.
  - Phase 11 code-quality re-review after replacement rollback fix: fixes required.
    - Stale UPDATE cleanup after reentrant `stop()`/restart can resurrect saved replaced/withdrawn routes because the session-generation mismatch path uses the full rollback path.
    - Required fix: on session-generation mismatch, remove only routes inserted by the stale UPDATE using update-id guards; do not restore saved replaced/withdrawn routes from the old generation. Add reentrant stop/restart coverage for replacement and withdrawal cases.
  - Stale UPDATE cleanup fix commit: `a105cd79d46ab151816412652a86870e01699358` `Fix stale update cleanup after reentrant teardown`.
    - Split stale generation cleanup from full rollback: stale session mismatch now removes only inserted routes by update ID and does not restore old saved routes.
    - Added reentrant tests for IPv4 replacement + stop, IPv4 withdrawal + restart, and IPv6 withdrawal + restart.
    - FSM suite now has 64 tests.
  - Controller verification after stale UPDATE cleanup fix: passed normal, headers, THREADSAFE, and sanitizer gates.
  - Phase 11 spec re-review after stale cleanup fix: passed.
  - Phase 11 code-quality re-review after stale cleanup fix is in progress.
  - Phase 11 code-quality re-review after stale cleanup fix: fixes required.
    - Duplicate same-prefix NLRI/MP_REACH entries inside one UPDATE can create replacement chains (`old -> A -> B`) and current replacement restore order can restore `A` instead of the pre-UPDATE `old` route after a later non-invalid failure.
    - Required fix: make duplicate-prefix replacement rollback restore the original pre-UPDATE route, with IPv4 and IPv6 regression tests.
  - Duplicate-prefix replacement rollback fix commit: `63fc346dc9f7f11846aeffa1492f54ea631a9505` `Fix duplicate route rollback order`.
    - Restores saved replacement routes in forward journal order so rollback leaves the original pre-UPDATE route after duplicate-prefix replacement chains.
    - Added IPv4 and IPv6 duplicate-prefix replacement rollback regression tests.
    - FSM suite now has 66 tests.
  - Controller verification after duplicate-prefix replacement rollback fix: passed normal, headers, THREADSAFE, and sanitizer gates.
  - Phase 11 spec re-review after duplicate-prefix rollback fix: fixes required.
    - Duplicate same-prefix advertisements for a previously absent prefix can still leave stale routes after rollback: rollback removes the final insertion, then restores an intermediate route created by the same failed UPDATE.
    - Required fix: do not restore replacements produced by the same failed UPDATE, or otherwise track pre-UPDATE presence; add IPv4 and IPv6 regression tests for absent-prefix duplicate replacement rollback.
  - Absent-prefix duplicate replacement rollback fix commit: `62694d290cff6514ebdbfe62ca27bf876c130d3d` `Fix absent duplicate update rollback`.
    - Added IPv4 and IPv6 tests proving duplicate-prefix advertisements for previously absent prefixes leave no route after non-invalid UPDATE rollback.
    - Added private RIB helpers to identify saved replacement route update IDs without exposing or traversing RIB internals in FSM.
    - FSM suite now has 68 tests.
  - Controller verification after absent-prefix duplicate replacement rollback fix: passed normal, headers, THREADSAFE, sanitizer, and FSM encapsulation grep gates.
  - Phase 11 spec re-review after absent-prefix duplicate rollback fix: passed.
  - Phase 11 code-quality re-review after absent-prefix duplicate rollback fix: fixes required.
    - Accepted finding: valid UPDATE route events are published before refreshing `last_rx_ms`, so a route-event callback or concurrent tick can falsely fire hold timeout after accepted UPDATE mutation.
    - Accepted finding: periodic KEEPALIVE send failure from `libbgp_fsm_tick()` returns an error but leaves OPEN_CONFIRM/ESTABLISHED state and learned routes intact.
    - Accepted finding: IPv6 MP_REACH/MP_UNREACH is accepted without MP-BGP capability negotiation, while local OPEN advertises only 4-byte ASN capability.
    - Accepted finding: private RIB transaction helpers have external linkage and can leak into the shared ABI because builds do not hide non-`LIBBGP_API` symbols.
    - Accepted finding: default config uses local BGP ID 0, so `libbgp_fsm_start()` can emit an invalid OPEN unless users override the default.
    - Minor findings deferred unless touched: unreachable state-specific NOTIFICATION branches and future split of large `src/fsm.c`.
  - Required code-quality fixes dispatched to worker:
    - Refresh RX time before route event callbacks for accepted UPDATEs, preserving stale/reentrant guards.
    - Teardown current sessions on periodic KEEPALIVE send failure, including route discard and `SESSION_DOWN` when ESTABLISHED.
    - Add MP-BGP IPv6 config/capability negotiation state; advertise IPv6 unicast MP capability when enabled and reject/ignore unnegotiated MP attributes.
    - Hide internal symbols from shared builds while keeping public `LIBBGP_API` symbols exported.
    - Reject `libbgp_fsm_start()` / passive local OPEN send when local BGP ID is 0.
  - Code-quality fix commit: `3001dab7cfc42fa5a7f4933b52f2a63659fb5d83` `Fix Phase 11 FSM quality issues`.
    - Added `enable_mpbgp_ipv6` FSM config, MP-BGP IPv6 OPEN advertisement, and peer capability tracking/rejection for unnegotiated IPv6 MP UPDATEs.
    - Refreshed accepted UPDATE RX timestamp before route-event callbacks.
    - Added current-session teardown on periodic KEEPALIVE send failure.
    - Builds with `LIBBGP_SHARED` and `-fvisibility=hidden`; public `LIBBGP_API` symbols remain exported and private RIB helpers are hidden.
    - Rejects start/passive local OPEN send when local BGP ID is 0.
    - FSM suite now has 75 tests.
  - Controller verification after code-quality fix: passed normal, headers, THREADSAFE, sanitizer, encapsulation grep, and dynamic symbol checks.
  - Phase 11 spec re-review after code-quality fix: passed.
  - Phase 11 code-quality re-review after code-quality fix: fixes required.
    - Accepted finding: some teardown paths publish `SESSION_DOWN` before sending the old session's NOTIFICATION, so a reentrant `SESSION_DOWN` callback can restart the FSM and then receive a stale NOTIFICATION after the new OPEN.
    - Accepted finding: `libbgp_fsm_destroy()` frees an established FSM without discarding peer-learned routes from borrowed RIBs.
    - Minor finding deferred: future split of large `src/fsm.c`.
  - Required fix dispatched to worker:
    - Send protocol NOTIFICATION before `SESSION_DOWN` callbacks, or otherwise generation-guard stale sends, with reentrant restart tests for stop and invalid UPDATE teardown.
    - Make `libbgp_fsm_destroy()` discard peer-learned routes while peer state is still known, without publishing events or sending on the out handler from a destructor.
  - Teardown ordering/destroy cleanup fix commit: `81ccedb5dfa8350f04dc7e6df8af5ee9369481e1` `fix fsm teardown ordering`.
    - Sends old-session NOTIFICATION before `SESSION_DOWN` callbacks on stop and invalid UPDATE teardown paths.
    - `libbgp_fsm_destroy()` now discards established peer-learned RIB4/RIB6 routes without event publication or NOTIFICATION send.
    - Added reentrant restart ordering tests for stop and invalid UPDATE teardown, plus destroy route cleanup coverage.
    - FSM suite now has 78 tests.
  - Controller verification after teardown ordering/destroy cleanup fix: passed normal, headers, THREADSAFE, sanitizer, encapsulation grep, and dynamic symbol checks.
  - Phase 11 spec re-review after teardown ordering/destroy cleanup fix: passed.
  - Phase 11 code-quality re-review after teardown ordering/destroy cleanup fix is in progress.
  - Phase 11 code-quality re-review after teardown ordering/destroy cleanup fix: fixes required.
    - Accepted finding: `fsm_on_open_sent()` and passive-open handling send local KEEPALIVE while state remains `OPEN_SENT`, so a reentrant/concurrent peer KEEPALIVE during send can be treated as a bad packet and tear down a valid negotiation.
    - Accepted finding: local `hold_time` is emitted without validating that it is 0 or at least 3.
    - Accepted finding: `local_asn > 65535` with `enable_4byte_asn == false` silently truncates the OPEN ASN field.
  - Required fix dispatched to worker:
    - Commit negotiated peer fields and `OPEN_CONFIRM` before dropping the lock for local KEEPALIVE send; teardown on send failure only if generation still matches, with active and passive reentrant peer KEEPALIVE tests.
    - Reject invalid local config before sending local OPEN: `hold_time` must be 0 or at least 3; large ASNs require 4-byte ASN enabled.
    - Update tests that used local hold time 1 to use legal values while preserving timer assertions.
  - `progress.md` is intentionally controller-owned and excluded from worker changes.
  - OPEN negotiation reentrancy/local config fix commit: `75d8e596fdfd106507a98262dd21d943e56d298c` `Fix FSM open negotiation reentrancy`.
    - Active OPEN and passive OPEN now commit negotiated peer fields and transition to `OPEN_CONFIRM` before releasing the lock to send the local KEEPALIVE.
    - KEEPALIVE send failure rollback is guarded by the captured session generation.
    - Local OPEN config validation now rejects hold times 1-2 and large local ASNs without 4-byte ASN capability before sending local OPEN.
    - Added active/passive reentrant peer KEEPALIVE tests and local config validation coverage for start/passive OPEN.
    - Updated timer tests to use legal hold times while preserving keepalive/hold timeout assertions.
    - FSM suite now has 84 tests.
  - Controller verification after OPEN negotiation reentrancy fix: passed focused FSM, normal, headers, THREADSAFE, sanitizer, encapsulation grep, and dynamic symbol checks.
  - Phase 11 spec review after OPEN negotiation reentrancy/local config fix: passed.
  - Phase 11 code-quality review after OPEN negotiation reentrancy/local config fix: fixes required.
    - Accepted finding: passive OPEN still sends local OPEN while state is `OPEN_SENT`, so a reentrant/concurrent peer KEEPALIVE during that local OPEN send can be handled as OpenSent bad type and tear down a valid negotiation.
    - Accepted finding: existing passive reentrant KEEPALIVE test injects only during outbound local KEEPALIVE, not during outbound local OPEN send.
  - Required fix dispatched to worker:
    - Add a passive reentrant peer KEEPALIVE test that injects during outbound local OPEN send and fails before the fix.
    - Rework passive OPEN so the local OPEN send window cannot reject a legitimate peer KEEPALIVE, while preserving generation-guarded rollback for send failures and reentrant restart/stop paths.
  - Passive OPEN local-OPEN-send reentrancy fix commit: `4c51cd3216df7ec735e06324c262ad8aa53ded0a` `Fix passive OPEN send reentrancy`.
    - Passive OPEN now commits negotiated peer fields and enters `OPEN_CONFIRM` before sending local OPEN, avoiding the transient `OPEN_SENT` bad-type window.
    - Added a passive reentrant peer KEEPALIVE test that injects during outbound local OPEN send.
  - Controller local review found a remaining ordering gap:
    - If peer KEEPALIVE arrives during outbound local OPEN send, the FSM established immediately and skipped the local KEEPALIVE response.
    - Required fix: defer establishment until after the outer passive OPEN path successfully sends local KEEPALIVE, while preserving the no-teardown reentrant behavior.
  - Passive OPEN keepalive ordering fix commit: `e604fa93debb1be326b62a0484d46209798e9bf8` `Fix passive open keepalive ordering`.
    - Records peer KEEPALIVE received during passive local OPEN send as pending.
    - Sends local KEEPALIVE before transitioning to ESTABLISHED and publishing SESSION_UP when the original generation still matches.
    - Tightened the new reentrant test to assert both local OPEN and local KEEPALIVE are sent.
    - FSM suite now has 85 tests.
  - Controller verification after passive OPEN reentrancy fixes: passed focused RED/GREEN, normal, headers, THREADSAFE, sanitizer, encapsulation grep, and dynamic symbol checks.
  - Phase 11 spec review after passive OPEN reentrancy fixes: passed.
  - Phase 11 code-quality review after passive OPEN reentrancy fixes: fixes required.
    - Accepted finding: if the local KEEPALIVE send is reentered by a peer KEEPALIVE and then the send fails, active/passive negotiation can remain `ESTABLISHED` even though the local KEEPALIVE was not successfully sent.
    - Required fix: make failed local KEEPALIVE send authoritative for the current negotiation attempt, or defer reentrant promotion until the send returns successfully, with active and passive regression tests where the send callback reenters with peer KEEPALIVE and returns failure.
  - Reentrant local KEEPALIVE send-failure rollback fix commit: `bfb8c33e9f8b4a7ec74a2c6a8964907bcb20fcd4` `Fix reentrant keepalive send rollback`.
    - Added active and passive tests for peer KEEPALIVE reentry during local KEEPALIVE send followed by send failure.
    - Rolls back the same negotiation attempt even if reentrant KEEPALIVE promoted it to `ESTABLISHED`, while preserving newer stop/start generations.
    - FSM suite now has 87 tests.
  - Controller verification after reentrant local KEEPALIVE rollback fix: passed normal, headers, THREADSAFE, sanitizer, encapsulation grep, and dynamic symbol checks.
  - Phase 11 spec re-review after reentrant local KEEPALIVE rollback fix: passed.
  - Phase 11 code-quality re-review after reentrant local KEEPALIVE rollback fix: fixes required.
    - Accepted finding: reentrant peer KEEPALIVE can publish `SESSION_UP` while the outer local KEEPALIVE send is still in progress; if that send then fails, state rolls back to `IDLE` after `SESSION_UP` escaped.
    - Accepted finding: a `SESSION_UP` callback could run while the FSM is briefly `ESTABLISHED` and mutate attached RIBs before rollback, leaving stale side effects.
    - Required fix: defer `SESSION_UP` and any established behavior until the local KEEPALIVE send succeeds, with active/passive event-bus and RIB-side-effect regression coverage.
  - Deferred `SESSION_UP` fix commit: `8259114cae5b01e74d974ca337b785ebf1285611` `Defer session up until local keepalive succeeds`.
    - Reentrant peer KEEPALIVE during local KEEPALIVE send now records pending state instead of publishing `SESSION_UP`.
    - Successful local KEEPALIVE send promotes pending peer KEEPALIVE to `ESTABLISHED` and publishes `SESSION_UP` after send completion.
    - Failed local KEEPALIVE send rolls back to `IDLE` without `SESSION_UP`, `SESSION_DOWN`, or RIB side effects from session-up callbacks.
    - Added active/passive event leakage assertions and active RIB side-effect regression coverage.
  - Controller verification after deferred `SESSION_UP` fix: passed normal, headers, THREADSAFE, sanitizer, encapsulation grep, and dynamic symbol checks.
  - Phase 11 spec re-review after deferred `SESSION_UP` fix: passed.
  - Phase 11 code-quality re-review after deferred `SESSION_UP` fix: passed.

- Phase 11: FSM complete.
- Phase 12: Umbrella header, examples, README.
  - Implementation commit: `2e7b5392f9199e3dffc1e83ed7d826db99aa625a` `Add C examples and umbrella header`.
  - Added `include/libbgp/libbgp.h`.
  - Added C examples:
    - `examples/peer_and_print.c`
    - `examples/route_server.c`
  - Rewrote `examples/README.md`.
  - Added explicit `make examples` target building examples under `build/threadsafe-$(THREADSAFE)/examples/`.
  - Removed old C++ examples after C examples built:
    - `examples/deserialize-and-serialize.cc`
    - `examples/peer-and-print.cc`
    - `examples/route-event-bus.cc`
    - `examples/route-filter.cc`
    - `examples/route-server.cc`
  - Worker verification: `make examples`, example `--help` smoke tests, `make headers`, `make test`, and `git diff --check` passed.
  - Controller verification: normal examples/help/bad-arg smoke tests, headers, normal tests, THREADSAFE examples/tests, sanitizer tests/examples, sanitizer example `--help` smoke tests, `git diff --check`, and final `make clean` passed.
  - Spec review: passed.
  - Code-quality review: fixes required.
    - Accepted finding: examples using fd out-handler should suppress default `SIGPIPE` so peer disconnects return errors instead of terminating the process.
    - Accepted minor finding: `route_server` should handle event-bus subscription allocation failures instead of silently disabling event printing.
  - Required fix dispatched to worker:
    - Add simple SIGPIPE handling in both examples.
    - Check `libbgp_event_bus_subscribe()` results in `route_server` and clean up on failure.
  - Code-quality fix commit: `069dcba09057164b8e0b86736afbd0dc44415ddd` `Fix example SIGPIPE and event subscribe handling`.
    - Added early `signal(SIGPIPE, SIG_IGN)` setup in both examples.
    - Added checked route-server event subscription setup with cleanup on failure.
  - Controller verification after code-quality fix: passed normal examples/help/bad-arg smoke tests, headers, normal tests, THREADSAFE examples/tests, sanitizer tests/examples, sanitizer example `--help` smoke tests, `git diff --check`, and final `make clean`.
  - Code-quality re-review: fixes required.
    - Accepted finding: examples build BGP router IDs via byte-copy into `uint32_t`, but `libbgp_open_write()` serializes `bgp_id` as a big-endian numeric value.
    - Accepted finding: `route_server` uses one IPv4 print helper for both byte-stored prefixes and numeric BGP router IDs.
  - Required endian fix dispatched to worker:
    - Build BGP router IDs numerically with shifts in both examples.
    - Split route-server printing helpers for numeric router IDs vs byte-stored prefix addresses.
  - Endian fix commit: `00ea334bcac0661dfe0401a52ee8a70bdb5cf3e5` `Fix example router ID endian handling`.
    - Replaced memcpy-based router ID construction with numeric big-endian shifts.
    - Split route-server IPv4 printing so `source_router_id` prints as numeric router ID while prefix4 addresses keep byte-storage printing.
  - Controller verification after endian fix: passed normal examples/help/bad-arg smoke tests, headers, normal tests, THREADSAFE examples/tests, sanitizer tests/examples, sanitizer example `--help` smoke tests, `git diff --check`, and final `make clean`.
  - Spec re-review after fixes: passed.
  - Code-quality re-review after endian fix: passed.

- Phase 12: umbrella header, examples, README complete.
- Phase 13: final cutover and C++/Autotools removal.
  - Pre-removal release gates started.
  - Passed before issue:
    - `rtk make clean`
    - `rtk make`
    - `rtk make headers`
    - `rtk make test`
    - `rtk make THREADSAFE=1 test`
    - `rtk make clean`
    - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
  - RED finding: running plain `rtk make examples` immediately after sanitizer test failed because `build/threadsafe-0` reused sanitizer-instrumented library objects while the plain example link omitted sanitizer runtime.
  - Root cause: `BUILD_DIR` is keyed only by `THREADSAFE`; objects do not rebuild when `CFLAGS_EXTRA`/`LDFLAGS_EXTRA` change between invocations.
  - Build-flag isolation fix dispatched to worker before C++/Autotools removal.
  - Build-flag isolation fix commit: `258bb352fd30098fbcfa958151857e25e9861a46` `Fix flag-change rebuilds`.
    - Added `build/threadsafe-$(THREADSAFE)/.flags` stamp containing effective `CC`, `CPPFLAGS`, `CFLAGS`, and `LDFLAGS_EXTRA`.
    - Made object files depend on the stamp so switching plain/sanitizer flags rebuilds incompatible objects while preserving existing output paths.
  - Controller regression check after build-flag fix:
    - `rtk make clean`
    - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
    - `rtk make examples` passed without manual clean.
  - Build-flag fix spec review: passed.
    - Verified `.flags` stays under `build/threadsafe-$(THREADSAFE)`, records effective compile/link flags, and preserves documented output paths.
  - Build-flag fix code-quality review: fixes required.
    - Important: `.flags` recipe single-quotes expanded flag values; valid flags containing single quotes can break the shell recipe or record a different value than the actual compile/link command.
    - Minor noted but accepted for now: `LDFLAGS_EXTRA` changes force object rebuilds as a conservative implementation.
  - Build-flag quote-robustness fix: dispatched.
  - Build-flag quote-robustness fix commit: `165e3f8478674da19be654df88e6122dd5b7fb14` `Fix flag stamp quoting`.
    - Replaced shell-single-quoted stamp generation with GNU make `$(file ...)` writing a make-native `FLAG_STAMP_CONTENT` block.
  - Controller regression check after quote fix:
    - `rtk make clean`
    - `rtk make CFLAGS_EXTRA="-DNAME='a b'" build/threadsafe-0/.flags`
    - `rtk sed -n '1,8p' build/threadsafe-0/.flags` showed the single-quoted flag preserved in stamp content.
    - Repeating the same stamp target exited cleanly without rewriting due to unchanged content.
    - `rtk make clean`
    - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
    - `rtk make examples`
    - Repeating `rtk make examples` caused archive/example relinks but no object rebuild loop.
    - `rtk git diff --check HEAD~1..HEAD`
  - Build-flag quote fix spec re-review: passed.
  - Build-flag quote fix code-quality re-review: fixes required.
    - Important: forced `.flags` rule creates/removes `.flags.tmp` in `$(BUILD_DIR)` and updates directory mtime; because library targets list `$(BUILD_DIR)` as a normal prerequisite, unchanged builds rearchive/relink every time.
    - Important: GNU make `$(file ...)` executes under `make -n`, causing dry runs to create/update `.flags.tmp`.
    - Minor accepted: Makefile now assumes GNU make via `$(file ...)`.
  - Build-flag incremental/dry-run cleanup fix: dispatched.
  - Build-flag incremental/dry-run cleanup fix commit: `a619bf0d46560326a4bd52eb75a9e916b167b76f` `Fix flag stamp incremental rebuilds`.
    - Moved temporary stamp file outside `$(BUILD_DIR)`.
    - Replaced GNU make `$(file ...)` with shell `printf` over exported `FLAG_STAMP_CONTENT`, avoiding dry-run file writes.
    - Made library build-directory prerequisites order-only so build-directory mtime changes do not force rearchive/relink loops.
  - Controller regression check after incremental/dry-run cleanup:
    - `rtk make clean`
    - `rtk make -n build/threadsafe-0/.flags`
    - `rtk bash -lc 'test ! -e build && test ! -e build/.flags.threadsafe-0.tmp'`
    - `rtk make examples`
    - Repeating `rtk make examples` returned `make: ok` with no compile/archive/link commands.
    - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
    - `rtk make examples` after sanitizer test rebuilt plain objects and linked successfully.
    - `rtk make clean`
    - `rtk make CFLAGS_EXTRA="-DNAME='a b'" build/threadsafe-0/.flags`
    - `rtk sed -n '1,8p' build/threadsafe-0/.flags` showed `-DNAME='a b'` preserved.
    - `rtk make clean`
    - `rtk make -n build/threadsafe-0/.flags`
    - `rtk bash -lc 'test ! -e build && test ! -e build/.flags.threadsafe-0.tmp'`
    - `rtk git diff --check HEAD~1..HEAD`
  - Build-flag incremental/dry-run cleanup spec re-review: passed.
  - Build-flag incremental/dry-run cleanup code-quality re-review: passed.
    - Minor noted and accepted: two separate concurrent `make` processes in the same worktree and same `THREADSAFE` can race on the fixed temp stamp path; this is not a `make -j` race in one invocation and does not block Phase 13.
  - Build-flag Makefile fix complete; ready to rerun Phase 13 pre-removal release gates.
  - Phase 13 pre-removal release gates after Makefile flag fixes passed:
    - `rtk make clean`
    - `rtk make`
    - `rtk make headers`
    - `rtk make test`
    - `rtk make THREADSAFE=1 test`
    - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
    - `rtk make examples`
  - Legacy C++/Autotools removal commit: `68976ada0e206c5d9594b290d6069f4b334d87c9` `release: cut over libbgp to pure c11 implementation`.
    - Removed Autotools files, tracked `src/*.cc`, legacy `src/bgp-*.h`, `src/bgp.h`, `src/fd-out-handler.h`, `prefix*` legacy headers, clock/route-event/serializable/value-op headers, and `src/libbgp.swg`.
    - Worker verification after removal passed clean/build/headers/test/THREADSAFE/sanitizer/examples/diff-check gates.
  - Legacy removal spec review: passed.
    - Verified commit message, deletions-only diff, removed requested patterns, and preserved C11 helper headers.
  - Legacy removal code-quality review: fixes required.
    - Important: `README.md` still describes Autotools/g++ and deleted `./autogen.sh && ./configure && make` flow as primary install path.
    - Important: `README.md` still describes removed C++ API/examples (`BgpPacket`, `BgpFsm`, `RouteEventBus`, `.cc`/`c++`) as current interface.
    - Minor: `Doxyfile` still brands project as a C++ BGP library and keeps stale C++ file-pattern defaults.
  - Phase 13 documentation cutover cleanup: dispatched.
  - Phase 13 documentation cutover cleanup commit: `9351638ed2e63b03caf4f904b83f1ae9dae47008` `docs: update C11 build documentation`.
    - Updated `README.md` to describe the C11 library, umbrella header, root `Makefile` flow, examples, `THREADSAFE=1`, and sanitizer flags.
    - Updated `Doxyfile` project brief/output mode/input patterns for C11/C docs.
    - Worker verification passed stale Autotools/C++ grep, headers, examples, diff-check, and status checks.
  - Documentation cutover spec review: passed.
  - Documentation cutover code-quality review: fixes required.
    - Important: `Doxyfile` uses `INPUT = ./include ./src README.md` with `RECURSIVE = NO`, so Doxygen will not descend into `include/libbgp/` and public headers may be omitted.
  - Doxyfile public-header input fix: dispatched.
  - Doxyfile public-header input fix commit: `760c50e8e20e5749d9101f3192d3283eeb9411e7` `Fix Doxygen public header input`.
    - Changed `INPUT` to `./include/libbgp ./src README.md` while keeping `RECURSIVE = NO`.
    - Worker noted optional `doxygen` run was skipped because `doxygen` is not installed.
  - Doxyfile public-header input fix spec re-review: passed.
  - Doxyfile public-header input fix code-quality re-review: passed.
  - Phase 13 implementation/review loops complete.
  - Final verification gates passed:
    - `rtk make clean`
    - `rtk make`
    - `rtk make headers`
    - `rtk make test`
    - `rtk make THREADSAFE=1 test`
    - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
    - `rtk make examples`
    - `rtk make clean`
    - `rtk make`
    - `rtk proxy nm -g build/threadsafe-0/libbgp.a | rtk proxy rg " T | D | B "` showed only public `libbgp_` symbols and internal `bgp_` symbols.
    - `rtk proxy nm -g build/threadsafe-0/libbgp.a | rtk proxy awk '$2 ~ /^[TDB]$/ && $3 !~ /^(libbgp_|bgp_)/ {print}'` produced no output.
    - `rtk proxy nm -D --defined-only build/threadsafe-0/libbgp.so | rtk proxy rg "libbgp_rib[46]_(saved_route|insert_save|withdraw_exact|restore_saved|exact_update)|bgp_hashmap"` produced no output.
    - `rtk proxy nm -D --defined-only build/threadsafe-0/libbgp.so | rtk proxy awk '$2 ~ /^[TDB]$/ && $3 !~ /^libbgp_/ {print}'` produced no output.
    - `rtk proxy rg -n "atexit|signal\\(|__thread|thread_local|std::|new |delete |class |namespace " include src` produced no output.
    - `rtk proxy rg -n "#include <pthread.h>|pthread_" include` produced no output.
    - `rtk proxy rg -n "std::|namespace |class |template<|throw |catch\\(|new |delete " include src tests examples` produced no output.
    - `rtk rg --files include src tests examples | rtk rg '\\.(cc|cpp|cxx|hpp|hh|swg)$'` produced no output.
    - `rtk rg -n "autogen|configure|automake|libtool|g\\+\\+|c\\+\\+|BgpPacket|BgpFsm|RouteEventBus|\\.cc" README.md Doxyfile examples` produced no output.
    - `rtk git diff --check` produced no output.
    - `rtk git status --short --branch` showed only untracked controller-owned `progress.md`.
  - Final whole-branch code review: dispatched.
  - Final whole-branch code review: fixes required.
    - Important: `Doxyfile` still included `./src`, so generated docs/source browser could include private implementation/internal files after public-header cutover.
    - Minor: `make headers` tested source-tree include strings (`"include/libbgp/foo.h"`) rather than installed-style `<libbgp/foo.h>`.
  - Final review fix commit: `780d487a8b4f628092423db0dc22c8e3a3a8ef89` `Fix public documentation and header checks`.
    - Changed `Doxyfile INPUT` to `./include/libbgp README.md`.
    - Changed `make headers` to compile installed-style `<libbgp/...>` includes with `-Iinclude`.
  - Controller focused verification after final review fix:
    - `rtk make headers`
    - `rtk rg -n "^INPUT\\s*=.*\\./src|^INPUT|^RECURSIVE" Doxyfile` showed `INPUT = ./include/libbgp README.md` and `RECURSIVE = NO`.
    - `rtk rg -n "#include <libbgp/" build/threadsafe-0/header_test.c` confirmed installed-style include generation.
    - `rtk git diff --check`
  - Final review fix spec re-review: passed.
  - Final review fix code-quality re-review: passed.
  - Final verification gates rerun at HEAD `780d487a8b4f628092423db0dc22c8e3a3a8ef89`: passed.
    - `rtk make clean`
    - `rtk make`
    - `rtk make headers`
    - `rtk make test`
    - `rtk make THREADSAFE=1 test`
    - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
    - `rtk make examples`
    - `rtk proxy nm -g build/threadsafe-0/libbgp.a | rtk proxy awk '$2 ~ /^[TDB]$/ && $3 !~ /^(libbgp_|bgp_)/ {print}'` produced no output.
    - `rtk proxy nm -D --defined-only build/threadsafe-0/libbgp.so | rtk proxy awk '$2 ~ /^[TDB]$/ && $3 !~ /^libbgp_/ {print}'` produced no output.
    - `rtk proxy nm -D --defined-only build/threadsafe-0/libbgp.so | rtk proxy rg "libbgp_rib[46]_(saved_route|insert_save|withdraw_exact|restore_saved|exact_update)|bgp_hashmap"` produced no output.
    - `rtk proxy rg -n "atexit|signal\\(|__thread|thread_local|std::|new |delete |class |namespace " include src` produced no output.
    - `rtk proxy rg -n "#include <pthread.h>|pthread_" include` produced no output.
    - `rtk proxy rg -n "std::|namespace |class |template<|throw |catch\\(|new |delete " include src tests examples` produced no output.
    - `rtk rg --files include src tests examples | rtk rg '\\.(cc|cpp|cxx|hpp|hh|swg)$'` produced no output.
    - `rtk rg -n "autogen|configure|automake|libtool|g\\+\\+|c\\+\\+|BgpPacket|BgpFsm|RouteEventBus|\\.cc" README.md Doxyfile examples` produced no output.
    - `rtk rg -n "^INPUT\\s*=.*\\./src|^INPUT|^RECURSIVE" Doxyfile` showed `INPUT = ./include/libbgp README.md` and `RECURSIVE = NO`.
    - `rtk git diff --check` produced no output.
    - `rtk git status --short --branch` showed only untracked controller-owned `progress.md`.
  - Final branch status: ready for finishing-development-branch workflow.

## Next

- Choose branch finishing option: local merge, push/PR, keep branch/worktree as-is, or discard.

## Verification Log

- `rtk make test` in the legacy baseline: failed as expected, no `test` target.
- Phase 0-2 worker reported passing:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
- Phase 0-2 fix worker reported passing:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
- Phase 0-2 vector fix worker reported passing:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
- Phase 3 worker reported passing:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
- Phase 3 fix worker reported passing:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
- Phase 4 worker reported passing:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
- Phase 5 worker reported passing:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
- Phase 5 fix worker reported passing:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
- Phase 6 worker reported passing:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
- Phase 6 fix worker reported passing:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
- Phase 6 quality fix worker reported passing:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
- Phase 6 final flag fix worker reported passing:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
- Phase 7 worker reported passing:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
- Phase 7 fix worker reported passing:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
- Phase 7 packet staging fix worker reported passing:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
- Phase 8 worker reported passing:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
- Phase 8 fix worker reported passing:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
- Phase 8 router-id fix worker reported passing:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
- Phase 9 worker reported passing:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
- Phase 9 fix worker reported passing:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
- Phase 9 teardown sync fix worker reported passing:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
- Phase 10 worker reported passing:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
- Phase 11 protocol cleanup controller verification passed:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make clean`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
- Phase 11 stop/session-down controller verification passed:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make clean`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
- Phase 11 keepalive-default controller verification passed:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make clean`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
- Phase 11 invalid-update hold-timer controller verification passed:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make clean`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
- Phase 11 code-quality fix controller verification passed:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make clean`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
- Phase 10 fix worker reported passing:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
- Phase 11 worker reported passing:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
- Phase 11 fix worker reported passing:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
- Phase 11 code-quality fix worker reported passing:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make clean && rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
- Phase 11 controller verification after code-quality fix passed:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make clean`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
- Phase 11 mandatory UPDATE attribute validation controller verification passed:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make clean`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
  - `rtk make clean`
- Phase 11 invalid UPDATE notification/teardown controller verification passed:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make clean`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
  - `rtk make clean`
- Phase 11 RIB-private-helper cleanup controller verification passed:
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make clean`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
  - `rtk make clean`
- Phase 11 replacement rollback controller verification passed:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make clean`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
  - `rtk make clean`
- Phase 11 stale UPDATE cleanup controller verification passed:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make clean`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
  - `rtk make clean`
- Phase 11 duplicate-prefix replacement rollback controller verification passed:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make clean`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
  - `rtk make clean`
- Phase 11 absent-prefix duplicate replacement rollback controller verification passed:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make clean`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
  - `rtk make clean`
  - `rtk rg -n "hashmap|offsetof|fsm_rib|impl_layout|impl_view|journal_route|libbgp_rib[46]_withdraw\\(" src/fsm.c` produced no output.
- Phase 11 code-quality fix controller verification passed:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make clean`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
  - `rtk make`
  - `rtk bash -lc 'nm -D --defined-only build/threadsafe-0/libbgp.so | grep libbgp_fsm_start || true'` showed public `libbgp_fsm_start`.
  - `rtk bash -lc 'nm -D --defined-only build/threadsafe-0/libbgp.so | grep -E "libbgp_rib[46]_(saved_route|insert_save|withdraw_exact|restore_saved|exact_update)" || true'` produced no output.
  - `rtk make clean`
- Phase 11 teardown ordering/destroy cleanup controller verification passed:
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make clean`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
  - `rtk make`
  - `rtk bash -lc 'nm -D --defined-only build/threadsafe-0/libbgp.so | grep libbgp_fsm_start || true'` showed public `libbgp_fsm_start`.
  - `rtk bash -lc 'nm -D --defined-only build/threadsafe-0/libbgp.so | grep -E "libbgp_rib[46]_(saved_route|insert_save|withdraw_exact|restore_saved|exact_update)" || true'` produced no output.
  - `rtk make clean`
- Phase 11 OPEN negotiation reentrancy/local config controller verification passed:
  - `rtk make build/threadsafe-0/tests/test_fsm && rtk build/threadsafe-0/tests/test_fsm` passed with 84 FSM tests.
  - `rtk git diff --check` produced no output.
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make clean`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
  - `rtk make`
  - `rtk bash -lc 'nm -D --defined-only build/threadsafe-0/libbgp.so | grep libbgp_fsm_start || true'` showed public `libbgp_fsm_start`.
  - `rtk bash -lc 'nm -D --defined-only build/threadsafe-0/libbgp.so | grep -E "libbgp_rib[46]_(saved_route|insert_save|withdraw_exact|restore_saved|exact_update)" || true'` produced no output.
  - `rtk rg -n "hashmap|offsetof|fsm_rib|impl_layout|impl_view|journal_route|libbgp_rib[46]_withdraw\\(" src/fsm.c` produced no output.
  - `rtk make clean`
- Phase 11 passive OPEN local-OPEN-send reentrancy controller verification passed:
  - RED check: after tightening `fsm_passive_open_reentrant_keepalive_during_local_open_send_establishes`, `rtk make build/threadsafe-0/tests/test_fsm && rtk build/threadsafe-0/tests/test_fsm` failed at `tests/test_fsm.c:1770: expected 2, got 1`.
  - GREEN focused check: `rtk make build/threadsafe-0/tests/test_fsm && rtk build/threadsafe-0/tests/test_fsm` passed with 85 FSM tests.
  - `rtk git diff --check` produced no output.
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make clean`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
  - `rtk make`
  - `rtk bash -lc 'nm -D --defined-only build/threadsafe-0/libbgp.so | grep libbgp_fsm_start || true'` showed public `libbgp_fsm_start`.
  - `rtk bash -lc 'nm -D --defined-only build/threadsafe-0/libbgp.so | grep -E "libbgp_rib[46]_(saved_route|insert_save|withdraw_exact|restore_saved|exact_update)" || true'` produced no output.
  - `rtk rg -n "hashmap|offsetof|fsm_rib|impl_layout|impl_view|journal_route|libbgp_rib[46]_withdraw\\(" src/fsm.c` produced no output.
  - `rtk make clean`
- Phase 11 reentrant local KEEPALIVE rollback controller verification passed:
  - RED check: worker reported the active regression failed before fix with final state `ESTABLISHED` instead of `IDLE`.
  - `rtk make build/threadsafe-0/tests/test_fsm && rtk build/threadsafe-0/tests/test_fsm` passed with 87 FSM tests.
  - `rtk git diff --check` produced no output.
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make clean`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
  - `rtk make`
  - `rtk bash -lc 'nm -D --defined-only build/threadsafe-0/libbgp.so | grep libbgp_fsm_start || true'` showed public `libbgp_fsm_start`.
  - `rtk bash -lc 'nm -D --defined-only build/threadsafe-0/libbgp.so | grep -E "libbgp_rib[46]_(saved_route|insert_save|withdraw_exact|restore_saved|exact_update)" || true'` produced no output.
  - `rtk rg -n "hashmap|offsetof|fsm_rib|impl_layout|impl_view|journal_route|libbgp_rib[46]_withdraw\\(" src/fsm.c` produced no output.
  - `rtk make clean`
- Phase 11 deferred `SESSION_UP` controller verification passed:
  - RED check: worker reported the active regression failed before fix with `expected 0, got 1` for leaked event count.
  - `rtk make build/threadsafe-0/tests/test_fsm && rtk build/threadsafe-0/tests/test_fsm` passed with 87 FSM tests.
  - `rtk git diff --check` produced no output.
  - `rtk make clean`
  - `rtk make test`
  - `rtk make headers`
  - `rtk make THREADSAFE=1 test`
  - `rtk make clean`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
  - `rtk make`
  - `rtk bash -lc 'nm -D --defined-only build/threadsafe-0/libbgp.so | grep libbgp_fsm_start || true'` showed public `libbgp_fsm_start`.
  - `rtk bash -lc 'nm -D --defined-only build/threadsafe-0/libbgp.so | grep -E "libbgp_rib[46]_(saved_route|insert_save|withdraw_exact|restore_saved|exact_update)" || true'` produced no output.
  - `rtk rg -n "hashmap|offsetof|fsm_rib|impl_layout|impl_view|journal_route|libbgp_rib[46]_withdraw\\(" src/fsm.c` produced no output.
  - `rtk make clean`
- Phase 12 controller verification passed:
  - `rtk make clean`
  - `rtk make examples`
  - `rtk build/threadsafe-0/examples/peer_and_print --help`
  - `rtk build/threadsafe-0/examples/route_server --help`
  - `rtk build/threadsafe-0/examples/peer_and_print --bad` exited 2 with usage.
  - `rtk build/threadsafe-0/examples/route_server --bad` exited 2 with usage.
  - `rtk make headers`
  - `rtk make test`
  - `rtk make THREADSAFE=1 examples`
  - `rtk make THREADSAFE=1 test`
  - `rtk make clean`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" examples`
  - `rtk build/threadsafe-0/examples/peer_and_print --help`
  - `rtk build/threadsafe-0/examples/route_server --help`
  - `rtk git diff --check` produced no output.
  - `rtk git status --short --branch` showed only untracked controller-owned `progress.md`.
  - `rtk make clean`
- Phase 12 code-quality fix controller verification passed:
  - `rtk make clean`
  - `rtk make examples`
  - `rtk build/threadsafe-0/examples/peer_and_print --help`
  - `rtk build/threadsafe-0/examples/route_server --help`
  - `rtk build/threadsafe-0/examples/peer_and_print --bad` exited 2 with usage.
  - `rtk build/threadsafe-0/examples/route_server --bad` exited 2 with usage.
  - `rtk make headers`
  - `rtk make test`
  - `rtk make THREADSAFE=1 examples`
  - `rtk make THREADSAFE=1 test`
  - `rtk make clean`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" examples`
  - `rtk build/threadsafe-0/examples/peer_and_print --help`
  - `rtk build/threadsafe-0/examples/route_server --help`
  - `rtk git diff --check` produced no output.
  - `rtk git status --short --branch` showed only untracked controller-owned `progress.md`.
  - `rtk make clean`
- Phase 12 endian fix controller verification passed:
  - `rtk make clean`
  - `rtk make examples`
  - `rtk build/threadsafe-0/examples/peer_and_print --help`
  - `rtk build/threadsafe-0/examples/route_server --help`
  - `rtk build/threadsafe-0/examples/peer_and_print --bad` exited 2 with usage.
  - `rtk build/threadsafe-0/examples/route_server --bad` exited 2 with usage.
  - `rtk make headers`
  - `rtk make test`
  - `rtk make THREADSAFE=1 examples`
  - `rtk make THREADSAFE=1 test`
  - `rtk make clean`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" examples`
  - `rtk build/threadsafe-0/examples/peer_and_print --help`
  - `rtk build/threadsafe-0/examples/route_server --help`
  - `rtk git diff --check` produced no output.
  - `rtk git status --short --branch` showed only untracked controller-owned `progress.md`.
  - `rtk make clean`

## Historical review: C refactor completeness/correctness audit

Status: historical read-only review completed before the subsequent parity-fix rounds. The findings below were migrated into `TODO.md` and have since been resolved or documented unless explicitly noted in the current status.

Reviewed against:
- Plan: `/home/kshi/workspace/code/test/libbgp-gpt/docs/superpowers/plans/libbgp-c11-conversion-plan-v4.md`
- Old C++ baseline: `dc18fd163a0566d415ac35e70a182236eaedf397`
- Then-current C branch: `codex` at `780d487a8b4f628092423db0dc22c8e3a3a8ef89`

Completed:
- Compared plan phases 5-13 against current C public headers and implementation.
- Spawned three read-only review agents for protocol/message coverage, RIB/FSM/filter/event coverage, and build/ABI/docs coverage.
- Confirmed current tree status before review was only untracked controller-owned `progress.md`.

Findings requiring fixes before calling the C refactor functionally complete:
- Filter rule order is reversed from the C++ implementation and plan Phase 9. Current C uses first-added rule priority; old C++ used last-added rule priority. The current test locks in the wrong behavior.
- FSM route ranking counts all AS_PATH segments for `as_path_len` and `origin_as`; the plan and old C++ best-route behavior only use AS_SEQUENCE.
- Path attribute flag validation rejects valid optional-transitive attributes with the Partial bit, and accepts unknown well-known/transitive attributes that old C++ rejected.
- UPDATE AS4 compatibility behavior is incomplete: no restore/downgrade/prepend equivalent for AS_PATH/AS4_PATH or AGGREGATOR/AS4_AGGREGATOR.
- Standalone UPDATE parsing/add_attr allows duplicate attrs and does not enforce mandatory attr semantics; FSM validates later, but UPDATE API is weaker than old C++ behavior.
- OPEN parsing silently skips unknown optional parameter types; old C++ rejected them.
- `rib4_insert_local`/`rib6_insert_local` replace duplicate local prefixes instead of rejecting duplicates like old C++ local insert.
- Event bus API matches the plan's "number of notified subscribers" wording, but not the old C++ publisher-exclusion/boolean-success-count semantics.
- Final symbol-gate command in the plan still references `build/libbgp.a`, while current Makefile outputs `build/threadsafe-$(THREADSAFE)/libbgp.a`.
- Internal RIB helper functions use public-looking `libbgp_` names in private headers/static archives; dynamic export check hides them, but archive globals remain confusing.
- There is no single aggregate `make verify`/`make release-check` target for all final gates.
- Public `out_handler.h` is POSIX-oriented via `ssize_t`, `<sys/types.h>`, and fd helpers; acceptable on POSIX, but not fully freestanding C11/DPDK-neutral.

Next steps:
- Fix high-priority semantic gaps first: filter order, AS_SEQUENCE-only route ranking, pattr flag/unknown validation, AS4 compatibility behavior.
- Add regression tests for each semantic gap before implementation changes.
- Then address medium build/API hygiene: duplicate local insert semantics, final gate target/path, internal helper naming/visibility, and POSIX API isolation decision.

## High-priority correctness fixes

Status: implementation completed for the high-priority TODO set.

Completed:
- Added RED regressions for filter rule priority, AS_SEQUENCE-only route metrics, path-attribute flag validation, unknown well-known attributes, UPDATE AS4 parsing/helpers, standalone UPDATE validation, packet AS4 parse context, and OPEN unknown optional parameters.
- Fixed filter evaluation to use last-added matching rule priority, matching the original C++ behavior.
- Fixed FSM route metrics to count only AS_SEQUENCE segments for AS path length and origin AS.
- Fixed path-attribute flag validation:
  - rejects unknown well-known attributes,
  - rejects unrelated flag bits,
  - accepts Partial only when valid for optional-transitive attributes.
- Added AS4-aware parse entry points:
  - `libbgp_pattr_parse_as4`
  - `libbgp_update_parse_as4`
  - `libbgp_packet_parse_as4`
- Added UPDATE AS4 compatibility helpers:
  - `libbgp_update_prepend_asn`
  - `libbgp_update_restore_as_path`
  - `libbgp_update_downgrade_as_path`
  - `libbgp_update_restore_aggregator`
  - `libbgp_update_downgrade_aggregator`
- Added standalone UPDATE validation via `libbgp_update_validate`, duplicate attr rejection in `libbgp_update_add_attr`, and mandatory attr checks during parse/write.
- Fixed OPEN parsing to reject unknown optional parameter types with `LIBBGP_ERR_BAD_TYPE`.

Verification so far:
- Initial RED run: `rtk make test` failed on missing `libbgp_pattr_parse_as4`, proving the new AS4 parse regression was active.
- After implementation: `rtk make test` passed.
- Final high-priority verification passed:
  - `rtk make clean`
  - `rtk make headers`
  - `rtk make test`
  - `rtk make THREADSAFE=1 test`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
  - `rtk make examples`
  - `rtk git diff --check` produced no output.
  - `rtk git status --short --branch` showed expected modified source/test files plus untracked controller-owned `TODO.md` and `progress.md`.
- After the final `src/update.c` robustness adjustments, reran:
  - `rtk make headers`
  - `rtk make test`
  - `rtk make THREADSAFE=1 test`
  - `rtk make CFLAGS_EXTRA="-fsanitize=address,undefined -g" LDFLAGS_EXTRA="-fsanitize=address,undefined" test`
  - `rtk make examples`
  - `rtk git diff --check` produced no output.
  - `rtk git status --short --branch` showed the same expected modified/untracked files.

Next steps:
- Address remaining medium-priority TODOs separately.
